Options -Indexes
RewriteEngine On

# Rota /tenis/{id} → tenis.php?id={id}
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^tenis/([a-z0-9\-]+)/?$ tenis.php?id=$1 [L,QSA]
